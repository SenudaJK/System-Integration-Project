spring.application.name=management

# Development Profile (enables SSL bypass for testing)
spring.profiles.active=dev

# Server Configuration
server.port=8080
server.error.include-message=always

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/fuelquota?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Waybig@!23
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=senudajayathilaka0@gmail.com
spring.mail.password=cqgo lsse qfju iyeq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.debug=true

# OTP Configuration
app.otp.expiration-minutes=5

# External Service Configuration
traffic.department.api.url=https://api.trafficdepartment.gov/verify
traffic.department.api.key=YOUR_API_KEY

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
